# Name your project! Project names should contain only lowercase characters
# and underscores. A good package name should reflect your organization's
# name or the intended use of these models
name: 'bq_dbt_project'
version: '1.0.0'
config-version: 2

# This setting configures which "profile" dbt uses for this project.
profile: 'bq_dbt_project'

# Variables for multi-client support
vars:
  # Default client configuration (Client A)
  client_name: "client_a"
  client_id: "{{ env_var('DBT_CLIENT_ID', 'client_a') }}"
  
  # Client-specific configurations
  client_a:
    dataset_suffix: ""
    table_prefix: "client_a_"
  client_b:
    dataset_suffix: ""
    table_prefix: "client_b_"

# These configurations specify where dbt should look for different types of files.
# The `model-paths` config, for example, states that models in this project can be
# found in the "models/" directory. You probably won't need to change these!
model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"  # directory which will store compiled SQL files
clean-targets:         # directories to be removed by `dbt clean`
  - "target"
  - "dbt_packages"

# Configuring models
# Full documentation: https://docs.getdbt.com/reference/model-configs

models:
  bq_dbt_project:
    # Config indicated by + and applies to all files under models/example/
    staging:
      +materialized: view
    marts:
      +materialized: view

seeds:
  bq_dbt_project:
    # Seeds will be created in the main dataset (DBT_VISHAL_DATASET)
    # Remove the +schema config to use the default dataset
